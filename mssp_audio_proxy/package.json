{
  "name": "mssp-audio-proxy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test",
    "cuts:generate": "node scripts/generateBakedCuts.mjs",
    "cuts:check": "node scripts/generateBakedCuts.mjs --check"
  },
  "devDependencies": {
    "wrangler": "^4.103.0"
  }
}
